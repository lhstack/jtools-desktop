{
  "name": "jtools-plugin-vue-ts-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "pack:jtp": "node scripts/build-jtp.mjs",
    "build:jtp": "vue-tsc --noEmit && vite build && node scripts/build-jtp.mjs"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "jszip": "^3.10.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vite-plugin-singlefile": "^2.3.0",
    "vue-tsc": "^2.2.0"
  }
}
